# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*.pyo
venv/
# If using Jython
*.class
.env
# Go build artifacts (bin/ also holds tracked project shell scripts, so ignore
# only the compiled binaries, not the whole directory).
/bin/gardynd
/bin/gardynd-armv6
/gardynd
